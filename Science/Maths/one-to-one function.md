#calculus #maths 
> A [[function]] $f$ is called a **one-to-one [[function]]** if it never takes on the same value twice; that is 
$$
f(x_1) \ne f(x_2) \text{ whenever } x_1 \ne x_2
$$

A [[function]] is one-to-one if an only if no horizontal intersects its graph more than once